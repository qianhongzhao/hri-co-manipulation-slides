\documentclass[aspectratio=169,11pt]{beamer}

\usepackage{amsmath,amssymb,bm}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{appendixnumberbeamer}
\usepackage{tikz}
\usepackage{xcolor}
\usetikzlibrary{arrows.meta,positioning,fit}

\definecolor{navy}{HTML}{062B63}
\definecolor{orange}{HTML}{F36F21}
\definecolor{lightblue}{HTML}{EAF2FA}
\definecolor{lightgreen}{HTML}{EAF4E3}
\definecolor{warmgray}{HTML}{F3F4F6}
\definecolor{darkgray}{HTML}{28323C}
\definecolor{muted}{HTML}{66717D}

\setbeamercolor{normal text}{fg=darkgray,bg=white}
\setbeamercolor{structure}{fg=navy}
\setbeamercolor{frametitle}{fg=white,bg=navy}
\setbeamercolor{title}{fg=white}
\setbeamercolor{subtitle}{fg=white}
\setbeamercolor{block title}{fg=white,bg=navy}
\setbeamercolor{block body}{fg=darkgray,bg=lightblue}
\setbeamercolor{alertblock title}{fg=white,bg=orange}
\setbeamercolor{alertblock body}{fg=darkgray,bg=warmgray}
\setbeamercolor{exampleblock title}{fg=white,bg=darkgray}
\setbeamercolor{exampleblock body}{fg=darkgray,bg=lightgreen}

\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{itemize item}{\color{orange}\small$\blacktriangleright$}
\setbeamertemplate{itemize subitem}{\color{navy}\scriptsize$\bullet$}
\setbeamertemplate{enumerate item}{\color{orange}\insertenumlabel.}
\setbeamertemplate{blocks}[rounded][shadow=false]
\setbeamertemplate{frametitle}{%
  \nointerlineskip
  \begin{beamercolorbox}[wd=\paperwidth,ht=0.82cm,dp=0.22cm,leftskip=0.48cm]{frametitle}
    \usebeamerfont{frametitle}\insertframetitle
  \end{beamercolorbox}}
\setbeamertemplate{footline}{%
  \leavevmode%
  \hbox{%
  \begin{beamercolorbox}[wd=.78\paperwidth,ht=2.6ex,dp=1.1ex,leftskip=.45cm]{author in head/foot}%
    \color{muted}\scriptsize Qianhong Zhao \;|\; Human--Robot Co-Manipulation
  \end{beamercolorbox}%
  \begin{beamercolorbox}[wd=.22\paperwidth,ht=2.6ex,dp=1.1ex,rightskip=.45cm]{date in head/foot}%
    \hfill\color{muted}\scriptsize \insertframenumber/\inserttotalframenumber
  \end{beamercolorbox}}}

\setbeamerfont{title}{size=\fontsize{25}{29},series=\bfseries}
\setbeamerfont{subtitle}{size=\fontsize{14}{18}}
\setbeamerfont{frametitle}{size=\fontsize{15.5}{18},series=\bfseries}
\setbeamerfont{block title}{size=\normalsize,series=\bfseries}
\setbeamerfont{block body}{size=\small}

\newcommand{\key}[1]{\textcolor{orange}{\textbf{#1}}}
\newcommand{\pinkcomment}[1]{\textcolor{pink}{\textbf{#1}}}
% 你上一次把我的 ”\newcommand{\pinkcomment}[1]{\textcolor{pink}{\textbf{#1}}}“ 删掉了再改的时候不要动这一行
\newcommand{\greencomment}[1]{\textcolor{green}{\textbf{#1}}}

% -----------------------------------------------------------------------------
% Design discussion archive (source only; intentionally not rendered in PDF)
%
% 1. Why use an explicit human-intention model?
% User question: Diffusion Co-Policy generates actions as a black box. Should the slide explain
% that an explicit intention/response-oriented controller could be better?
% Assistant response: Diffusion Co-Policy learns a joint human--robot action distribution from
% demonstrations, so intention remains implicit and is not separately identifiable. An explicit
% model is useful for explanation, candidate-action evaluation, and model-based safety, but is not
% automatically more accurate because it can add modeling error and needs diverse robot-action
% data. The strongest proposal is a hybrid: explicit control-relevant intent variables plus a
% learned response model for behavior that the structured model does not capture.
% User decision: Revise the visible slide to state this necessity while retaining the discussion.
%
% 2. Notation-summary page placement.
% User request: Move the notation-summary page to backup.
% Assistant response: Agreed. Moving it improves the main narrative, provided every main-deck
% formula keeps short local definitions at or before first use.
% User decision: Move it.
%
% 3. Response conditioning for Options A--C.
% User question: Should all structured intention options account for human response to the robot?
% Assistant response: Yes. Option A is response-aware through u^R in its game cost; Option B can
% use p(xi_H^* | h_t,c,tilde u^R); Option C can use f_H(x_O,c,tilde u^R;theta_H).
% Response prediction should be a shared layer around A--C, not an independent intent variable.
% User decision: Add a response-prediction layer between the primitive and Options A--C and update
% the following slides to match this hierarchy.
%
% 4. Generality of the local dynamical-system model.
% User question: Why show only rotation; should the model cover a more general case?
% Assistant response: The model should cover the full rigid-object pose. The general state model
% includes translation, rotation, and linear/angular velocity. Rotation was separated because
% orientation is not handled by ordinary Euclidean subtraction. A clearer slide should present a
% full pose model with explicit translation and orientation components.
% User decision: Revise the visible slide to avoid the rotation-only misunderstanding.
%
% 5. Relationship between response prediction and N-PACE.
% User question: Both appear to model how the human accommodates robot behavior. Are they the same?
% Assistant response: They share a closed-loop idea but differ technically. N-PACE is structured
% and game-theoretic: it assumes parameterized costs/policies, estimates peer cost parameters, and
% models peer learning. The response model is data-driven p(u^H | history,context,candidate u^R)
% and can predict accommodation without identifying a cost or learning law. They can be combined:
% N-PACE supplies interpretable intent/learning structure; the response model supplies a learned
% prior or residual for behavior N-PACE does not explain.
% User decision: Include this distinction when introducing the new response layer.
%
% 6. Meaning of calibrated uncertainty on held-out people.
% User question: The phrase is difficult to understand.
% Assistant response: Test confidence on participants excluded from training. If the model gives an
% 80 percent prediction region, unseen-participant actions should fall inside it about 80 percent
% of the time. Simpler visible wording: uncertainty estimates tested on unseen participants.
% User decision: Replace the original wording with simpler language.
%
% 7. Bayesian-update page placement.
% User request: Move the Bayesian equation to backup.
% Assistant response: Agreed. The main story only needs the verbal idea that observed wrench and
% object motion update a population prior; the equation remains useful for implementation questions.
% User decision: Move it.
%
% 8. Translation--rotation tradeoff in the first study.
% User question: This tradeoff was not introduced earlier.
% Assistant response: One route may require shorter translation but larger table rotation, while
% another may be longer with less rotation. Either introduce that tradeoff earlier or replace it
% with the already established phrase "coordinated translation and rotation around obstacles."
% User decision: Introduce it earlier or use clearer wording; use the clearer wording here.
% -----------------------------------------------------------------------------

\newcommand{\paperout}[1]{%
  \begin{beamercolorbox}[rounded=true,sep=0.55em,wd=\linewidth]{block body}
    \footnotesize\textbf{Expected paper:} #1
  \end{beamercolorbox}}

\title{Human Intention Modeling for Physical Human--Robot Co-Manipulation}
\subtitle{Control-relevant representations, inference methods,\\and an initial experimental study}
\author{Qianhong Zhao}
\date{Research Discussion with RISE Lab}

\begin{document}

% =============================================================================
% Main deck for the follow-up discussion: one-year digital-twin research plan
% =============================================================================

{\setbeamertemplate{footline}{}
\begin{frame}[plain]
  \begin{tikzpicture}[remember picture,overlay]
    \fill[warmgray] (current page.south west) rectangle (current page.north east);
    \fill[orange] (current page.north west) rectangle ([xshift=0.18cm]current page.south west);
    \fill[navy] ([xshift=0.82cm,yshift=-0.88cm]current page.north west)
      rectangle ([xshift=0.98cm,yshift=-2.72cm]current page.north west);
  \end{tikzpicture}
  \vspace{0.72cm}
  \hspace{0.78cm}{\color{navy}\fontsize{17.5}{22}\selectfont\bfseries
  A Primitive-Structured Digital Twin for\\[-0.03cm]\vspace{-0.5cm}
  \hspace{0.78cm}Industrial Human--Robot Co-Manipulation\par}

  \vspace{0.55cm}
  % \hspace{0.78cm}{\color{darkgray}\fontsize{13}{17}\selectfont
  % Initial one-year research plan: system architecture, sensing,\\
  % \hspace{0.78cm}human modeling, and safety-grounded control\par}
  \vfill
  \hspace{0.78cm}{\color{navy}\normalsize\textbf{Qianhong Zhao}\par}
  \vspace{0.52cm}
\end{frame}}

\begin{frame}{Human and robot capabilities complement each other}
\begin{columns}[T,onlytextwidth]
\column{0.47\textwidth}
\begin{block}{Why retain the human}
\begin{itemize}
  \item adapt to new object shapes and changing instructions;
  \item use dexterous hands and practical judgment;
  \item respond to unexpected contact or task changes.
\end{itemize}
\end{block}
\column{0.49\textwidth}
\begin{block}{Why add the robot}
\begin{itemize}
  \item provide strength, precision, and repeatability;
  \item reduce fatigue and physical load;
  \item execute accurate motion and force under constraints.
\end{itemize}
\end{block}
\end{columns}
\vspace{0.28cm}
\begin{alertblock}{Target application}
Focus on factory tasks that change often: full automation requires too much setup, while human-only work is tiring, risky, or inconsistent.
\end{alertblock}
\vspace{0.12cm}
\centering\textbf{The human is an adapting partner, not an external disturbance.}
% Original pink comment: move this page before the page explaining what a digital twin is and use
% simpler words; for example, "semantic" is unclear.
% Assistant response: Moved this motivation page before both digital-twin pages. Replaced semantic
% judgment, high-mix, brittle, and ergonomic language with concrete examples: changing instructions,
% new object shapes, unexpected contact, physical load, and setup cost.
\end{frame}

\begin{frame}{A digital twin is a live model of the physical task}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Continuously updated from sensors}
The model tracks the robot, human, shared object, contact, and task using synchronized motion, force/torque, robot-state, and context measurements.
\end{block}
\column{0.48\textwidth}
\begin{block}{Used before and during robot action}
The robot tests possible actions in the model, selects a useful action, executes it through a safety layer, and compares the measured outcome with the prediction.
\end{block}
\end{columns}
\vspace{0.28cm}
\begin{alertblock}{Working definition}
A digital twin is a synchronized, predictive, and continuously corrected digital model connected to the physical human--robot task in a closed loop.
\end{alertblock}
% Original pink comment: Use this page to explain what a digital twin is.
% Assistant response: Replaced the previous checklist with a direct definition and two operational
% properties: continuously updated from sensors and used to test actions before physical execution.
\end{frame}

\begin{frame}{A digital twin connects five layers in one synchronized loop}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{1. Physical system}
The human and robot jointly move a shared object in a real workspace with tools, supports, and obstacles.
\end{block}
\begin{block}{2. Synchronized state}
Combine robot joints/commands, force and torque, object and human motion, contact state, and task goal on one timeline.
\end{block}
\column{0.48\textwidth}
\begin{block}{3. Hybrid twin core}
Combine object/contact physics, online parameter estimation, a human primitive/response model, and prediction uncertainty.
\end{block}
\begin{block}{4. Counterfactual prediction}
For each candidate robot action, predict the human response, object motion, interaction force, task progress, and risk.
\end{block}
\end{columns}
\begin{alertblock}{5. Decision and safety}
Use layers 1--4 to compare candidate actions with a task-appropriate planner/controller. Enforce physical and safety limits; update the twin from new measurements.
\end{alertblock}
% Original pink question: Should planning use the information from the four layers above? MPC and
% N-PACE may be too limited as the only named choices.
% Assistant response: Yes. Layer 5 now consumes the synchronized state, hybrid model, and
% counterfactual predictions from layers 1--4. The visible text no longer commits to MPC or N-PACE;
% it leaves the planner/controller task-dependent while retaining an explicit safety check.
% Original pink comment: use simpler words in the physical layer and ask whether sensor input kinds
% should be mentioned there.
% Assistant response: Simplified the physical-system description. Sensor kinds belong in the
% synchronized-state layer rather than the physical layer, so they are listed explicitly in layer 2.
% Original pink comment: after this page, explain foreseeable tasks: sensor number/type,
% synchronization and fusion, primitive number/composition, a Co-Policy example, twin-core
% components, and collaboration with PhD students; reuse the following pages if possible.
% Assistant response: The following main-deck sequence now answers these questions: primitive
% definition/composition and Co-Policy example; primitive vocabulary and model I/O; episode data and
% synchronization/fusion; task-derived sensor requirements and minimum hardware; hybrid-core and
% control architecture; work packages; and task-specific collaboration roles.
\end{frame}

\begin{frame}{A task uses context to compose reusable interaction primitives}
\begin{alertblock}{Simple definition}
A \textbf{primitive} is a reusable, short part of an interaction, such as translate, rotate, hold, align, insert, or yield. The scene provides context; a sequence of primitives describes how the task is executed.
\end{alertblock}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Task representation}
\centering\textbf{Task execution is a context-conditioned composition of primitives.}\par
\vspace{0.14cm}
\raggedright
Context includes the object, goal, obstacles, contact locations, and physical constraints.
\end{block}
\column{0.48\textwidth}
\begin{block}{Illustrative table-carrying decomposition}
\[
\begin{gathered}
\text{translate}\rightarrow\text{rotate}\rightarrow\text{align}\\
\rightarrow\text{translate}\rightarrow\text{hold}
\end{gathered}
\]
Each primitive has a local goal, speed/force level, and human--robot role.
\end{block}
\end{columns}
\vspace{0.1cm}
\centering\textbf{The same primitive can be reused across transport, insertion, and assembly.}
\end{frame}

\begin{frame}{The primitive model connects task reasoning to control}

\centering
\resizebox{0.94\textwidth}{!}{%
\begin{tikzpicture}[
  node distance=0.58cm,
  box/.style={rounded corners=2pt,draw=navy,thick,fill=lightblue,align=center,minimum height=0.9cm,font=\small},
  learned/.style={box,draw=orange,fill=warmgray},
  control/.style={box,fill=lightgreen},
  arr/.style={-{Latex[length=2.2mm]},thick,draw=muted}
]
\node[box,text width=2.55cm] (context) {Task context\\geometry + goal};
\node[learned,text width=2.75cm,right=of context] (high) {VLM/VLA\\primitive sequence};
\node[learned,text width=3.15cm,right=of high] (state) {Primitive state\\mode + goal + role};
\node[learned,text width=3.35cm,right=of state] (response) {Action-conditioned response\\human + object + wrench};
\node[control,text width=2.9cm,right=of response] (control) {Safe planning/control\\robot action};
\draw[arr] (context)--(high);
\draw[arr] (high)--(state);
\draw[arr] (state)--(response);
\draw[arr] (response)--(control);
\end{tikzpicture}}
\vspace{0.16cm}
\textbf{Flow:} VLM/VLA interprets the task and proposes a primitive sequence; online sensing identifies the active primitive; the response model predicts consequences; the controller selects the robot action.
\vspace{0.16cm}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Initial vocabulary and model size}
Start with six modes as an initial vocabulary. Evaluate on separate trials from people and task settings not used for training. Merge indistinguishable modes; add one only if it improves prediction and control on these unseen trials.
\end{block}
\column{0.48\textwidth}
\begin{block}{Control-facing input and output}
Input: interaction history, task context, and a candidate robot action. Output: current/next primitive, future object motion, interaction wrench, and uncertainty.
\end{block}
\end{columns}
% Original pink question: What does "held-out" mean?
% Assistant response: Replaced the term with a direct definition: separate evaluation trials from
% people and task settings not used for training. These unseen trials test whether merging or adding
% a primitive improves generalization rather than only fitting the training data.
% Original pink question: Explain the flow chart. Does VLM understand the task and find the right
% primitive?
% Assistant response: Added a visible flow explanation. VLM/VLA interprets task context and proposes
% a primitive sequence; synchronized online sensing identifies which primitive is currently active;
% the response model predicts consequences; and the controller selects the robot action.
\end{frame}

\begin{frame}{A synchronized episode needs three data groups}
\begin{columns}[T,onlytextwidth]
\column{0.315\textwidth}
\begin{block}{Static context}
\begin{itemize}
  \item CAD and geometry;
  \item mass, inertia, material;
  \item grasp/contact locations;
  \item goal, tolerance, human role.
\end{itemize}
\end{block}
\column{0.315\textwidth}
\begin{block}{Time series}
\begin{itemize}
  \item robot state, command, torque;
  \item 6D interaction wrench;
  \item object pose and twist;
  \item human pose and contact mode.
\end{itemize}
\end{block}
\column{0.315\textwidth}
\begin{block}{Outcomes and labels}
\begin{itemize}
  \item success and completion time;
  \item human effort and force disagreement;
  \item safety events;
  \item strategy or role changes.
\end{itemize}
\end{block}
\end{columns}
\vspace{0.25cm}
\begin{alertblock}{Initial synchronization and fusion pipeline}
Hardware timestamp/clock sync $\rightarrow$ resample on a common timeline $\rightarrow$ transform to calibrated frames $\rightarrow$ fuse asynchronous measurements with an uncertainty-aware state estimator.
\end{alertblock}
\end{frame}

\begin{frame}{Sensor requirements depend on the selected factory task}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Necessary measurement categories}
\begin{itemize}
  \item robot state and commanded action;
  \item interaction force and torque;
  \item shared-object motion;
  \item human hand/arm motion or contact state;
  \item workspace and safety events.
\end{itemize}
\end{block}
\column{0.48\textwidth}
\begin{block}{Task-derived requirements}
\begin{itemize}
  \item enough range to avoid saturation;
  \item enough resolution to detect meaningful motion/force changes;
  \item latency below the relevant interaction time scale;
  \item synchronized timestamps and calibrated frames.
\end{itemize}
\end{block}
\end{columns}
\begin{alertblock}{Initial position}
Define sensor count and numerical specification after selecting the benchmark. Table transport and precision insertion require different sensing; exact hardware should be chosen with the robotics team from the task requirements.
\end{alertblock}
% Original pink comment: Avoid excessive sensor detail or presenting me as a hardware expert. Mention
% necessary requirements and examples, while emphasizing dependence on the factory task.
% Assistant response: Removed the equations and specific backdrivability/impedance hardware claim.
% The slide now gives measurement categories and task-derived selection principles, explicitly
% deferring sensor count and numerical specifications until the benchmark and available hardware are
% discussed with the robotics team.
\end{frame}

\begin{frame}{The hybrid twin core combines four complementary models}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Physics and contact dynamics}
Predict shared-object motion and interaction force under robot and human inputs while enforcing known geometry and mechanics.
\end{block}
\begin{block}{Online system identification}
Update uncertain mass, inertia, damping, contact, payload, and actuator parameters from synchronized measurements.
\end{block}
\column{0.48\textwidth}
\begin{block}{Human primitive and response model}
Estimate the current primitive and predict human/object responses conditioned on each candidate robot action.
\end{block}
\begin{block}{Learned residual and uncertainty}
Capture behavior not explained by the structured models and report confidence for risk-aware planning and fallback.
\end{block}
\end{columns}
\begin{alertblock}{Interfaces outside the core}
VLM/VLA interprets the task and proposes primitive sequences. A task-appropriate planning/control method evaluates twin predictions, while an independent safety layer enforces physical constraints before execution.
\end{alertblock}
% Original pink comment: Avoid listing MPC, MPPI, or N-PACE here; use general method categories.
% Assistant response: Replaced the algorithm list with a task-appropriate planning/control method.
% The slide now emphasizes interfaces and responsibilities rather than prematurely selecting a solver.
\end{frame}

\begin{frame}{Three work packages define a realistic one-year plan}
\begin{columns}[T,onlytextwidth]
\column{0.315\textwidth}
\begin{block}{WP1: Physical digital twin}
\textbf{Focus:} sensing, synchronization, state estimation, and hybrid dynamics.

\textbf{Deliverable:} validated testbed and dataset.
\end{block}
\column{0.315\textwidth}
\begin{block}{WP2: Human response twin}
\textbf{Focus:} primitive transitions, action-conditioned responses, and personalization.

\textbf{Deliverable:} prediction across people and task variations.
\end{block}
\column{0.315\textwidth}
\begin{block}{WP3: World model and control}
\textbf{Focus:} use the twin dataset to learn action-conditioned rollouts for prediction, planning, and control.

\textbf{Deliverable:} validated rollouts and a safe physical demonstration.
\end{block}
\end{columns}
\vspace{0.22cm}
\begin{alertblock}{Dependency between work packages}
WP1 supplies synchronized physical data to WP2 and WP3. Validate the structured twin first, then learn the world model, and connect its rollouts to planning and control only after prediction and uncertainty are tested.
\end{alertblock}
% Original pink comment: Use the rich digital-twin data to construct a world model and then support
% planning and control; also consider target conferences or journals for WP1 and WP2.
% Assistant response: WP3 is now explicitly a world-model-to-planning/control project fed by WP1's
% synchronized dataset. The slide states the dependency and validation order. It also lists candidate
% venue families for all three work packages; these are discussion targets, not commitments, and the
% final venue should follow the dominant contribution and project timing.
\end{frame}

% \begin{frame}{Candidate publication paths follow the dominant contribution}
% \begin{columns}[T,onlytextwidth]
% \column{0.315\textwidth}
% \begin{block}{WP1: System and dataset}
% \textbf{Contribution:} synchronized digital-twin testbed, validation, and reusable data.

% \textbf{Candidates:} IEEE CASE, T-ASE, or RA-L.
% \end{block}
% \column{0.315\textwidth}
% \begin{block}{WP2: Human modeling}
% \textbf{Contribution:} primitive/response representation, generalization, and human study.

% \textbf{Candidates:} HRI, RO-MAN, ICRA, or RA-L.
% \end{block}
% \column{0.315\textwidth}
% \begin{block}{WP3: World model and control}
% \textbf{Contribution:} action-conditioned rollouts and safe physical planning/control.

% \textbf{Candidates:} ICRA, IROS, or RA-L.
% \end{block}
% \end{columns}
% \vspace{0.3cm}
% \begin{alertblock}{Discussion position}
% These are candidate venue families, not commitments. The final target should follow the strongest validated contribution, paper maturity, and submission timing.
% \end{alertblock}
% \end{frame}

\begin{frame}{ASU collaboration connects the testbed, HRI, and control}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{My role: twin, learning, and control}
Define the digital-twin architecture and sensing/model interfaces; develop system identification, response/world models, and adaptive/safe control; lead closed-loop integration.
\end{block}
\begin{block}{Joint model development}
Translate HRI hypotheses into primitives and response variables; jointly train and compare models using the synchronized experimental data.
\end{block}
\column{0.48\textwidth}
\begin{block}{ASU HRI students: testbed and studies}
Lead testbed operation, sensor calibration, data collection, study protocols, participant experiments, human-centered metrics, and behavior interpretation.
\end{block}
\begin{block}{How we work together}
Students maintain the physical experiment and data pipeline; I connect the learned twin and controller to that interface; we jointly design benchmarks and run closed-loop evaluations.
\end{block}
\end{columns}
% Original pink comment: Based on the foreseeable tasks, explain potential collaborations with the
% PI's PhD students.
% Assistant response: The collaboration slide now maps concrete project ownership to control/system
% integration, HRI primitive and human-study design, learning models/VLM interfaces, and shared data
% collection and closed-loop evaluation.
% New pink comment retained: ask HRI students to work on the hardware side. What do you mean by
% learning collaboration and shared experimental work? Do his students and I work together?
% Assistant response: Yes. The proposed split is now explicit rather than hiding work under broad
% labels. ASU HRI students lead testbed operation, sensing/calibration, data collection, study design,
% and human-centered evaluation. I define the data/model interface and lead the digital twin,
% learning/control methods, and closed-loop integration. Primitive definitions, model comparison,
% benchmark design, and physical validation are joint tasks. This keeps me involved in the physical
% system without implying that I alone must build and maintain the entire HRI testbed.
\pinkcomment{do not push all the dirty work like data collection experiment to his students. 我也承担一些 说成一起干}
\end{frame}

\begin{frame}{Four decisions can define the first joint milestone}
\begin{enumerate}
  \item \textbf{Benchmark task:} Select one factory operation and write its goal, physical setup, task variation, and success criteria.
  \vspace{0.16cm}
  \item \textbf{Starting assets:} Inventory reusable robots, sensors, simulation models, datasets, and controllers at ASU and Michigan.
  \vspace{0.16cm}
  \item \textbf{ASU ownership:} ASU leads the testbed, human studies, data collection, system integration, and primary experimental validation.
  \vspace{0.16cm}
  \item \textbf{First paper milestone:} Choose a measurable initial contribution---testbed/data, primitive human model, or world-model-based control---and its target venue.
\end{enumerate}
\vspace{0.12cm}
\begin{alertblock}{Supporting role for Michigan}
Michigan can support method development, simulation studies, theoretical analysis, and independent evaluation on the shared data. A biweekly technical call and common repository keep this support aligned with ASU's experimental milestones.
\end{alertblock}
% Original pink comment: Consider the collaboration rhythm with the Michigan collaborator and explain
% the four bullets more concretely.
% Assistant response: Each decision now has an explicit output: a written task specification, a
% cross-site asset inventory, assigned ownership plus shared interfaces, and a measurable first-paper
% milestone. The proposed cadence separates biweekly ASU integration from a monthly ASU--Michigan
% technical call and adds shared repository/data-schema/action-item mechanisms.
% New pink comment retained: Mention that ASU is the major contributor. What role should Michigan
% have, and what tasks should it do? A rough idea is sufficient.
% Assistant response: ASU is now explicitly the primary experimental and system-integration site,
% responsible for the testbed, human studies, synchronized data, and closed-loop validation.
% Michigan is presented as a supporting collaborator for methods that can be developed without
% duplicating ASU's hardware effort: simulation, system identification, theoretical analysis, and
% independent evaluation on shared data. These are proposed roles to discuss, not claims about
% resources or commitments that have not yet been confirmed.
\end{frame}

\appendix
{\setbeamertemplate{footline}{}
\begin{frame}[plain]
\centering
\vfill
{\color{navy}\LARGE\bfseries Technical Background from the Previous Discussion}\par
\vspace{0.35cm}
{\color{muted}\large Human intention representations, response prediction, and initial studies}\par
\vfill
\end{frame}}

% =============================================================================
% Previous deck retained below as technical background and Q&A material
% =============================================================================

{\setbeamertemplate{footline}{}
\begin{frame}[plain]
  \begin{tikzpicture}[remember picture,overlay]
    \fill[warmgray] (current page.south west) rectangle (current page.north east);
    \fill[orange] (current page.north west) rectangle ([xshift=0.18cm]current page.south west);
    \fill[navy] ([xshift=0.82cm,yshift=-0.88cm]current page.north west)
      rectangle ([xshift=0.98cm,yshift=-2.72cm]current page.north west);
  \end{tikzpicture}
  \vspace{0.72cm}
  \hspace{0.78cm}{\color{navy}\fontsize{17.5}{22}\selectfont\bfseries
  Human Intention Modeling for\\[-0.03cm]\vspace{-0.5cm}
  \hspace{0.78cm}Physical Human--Robot Co-Manipulation\par}

  \vspace{0.55cm}
  \hspace{0.78cm}{\color{darkgray}\fontsize{13}{17}\selectfont
  Control-relevant representations, inference methods,\\
  \hspace{0.78cm}and an initial early stage study\par}
  \vfill
  \hspace{0.78cm}{\color{navy}\normalsize\textbf{Qianhong Zhao}\par}
  \vspace{0.12cm}
  % \hspace{0.78cm}{\color{muted}\small Research Discussion with RISE Lab\par}
  \vspace{0.52cm}
  
\end{frame}}

\begin{frame}{Discussion roadmap}
\begin{enumerate}
  \item \textbf{Problem definition}\par
  What must a control-relevant human model predict in physical co-manipulation?
  \vspace{0.18cm}
  \item \textbf{Lessons from the shared papers}\par
  What Diffusion Co-Policy, bounded-rational games, and PACE/N-PACE already establish---and what remains open.
  \vspace{0.18cm}
  \item \textbf{Proposed framework}\par
  A reusable intention representation plus robot-action-conditioned human response prediction.
  \vspace{0.18cm}
  \item \textbf{Path to a testable project}\par
  Candidate model realizations, data collection, evaluation, and a focused first study.
\end{enumerate}
\end{frame}

\begin{frame}{Reference task: Collaborative table carrying}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Task in Diffusion Co-Policy}
\begin{itemize}
  \item A human and a robot act at opposite ends of a rigid table.
  \item They move the table to a goal while avoiding obstacles.
  \item Each partner applies a planar force; success requires coordinated translation and rotation.
\end{itemize}
\end{block}
\column{0.48\textwidth}
\begin{block}{Information available online}
\begin{itemize}
  \item table pose and velocity;
  \item initial pose, goal, and obstacle locations;
  \item recent human force commands and robot actions;
  \item for physical co-manipulation: add measured 6D interaction wrench and human motion.
\end{itemize}
\end{block}
\end{columns}
\vspace{0.02cm}
\begin{alertblock}{Why an explicit human model is needed}
Diffusion Co-Policy maps history to joint actions, leaving intention implicit. An explicit model exposes intent, tests candidate robot actions, and connects prediction to planning and safety. A hybrid preserves learned flexibility.
\end{alertblock}

% User question: What if historical interaction data are unavailable? Should the method rely purely
% on online adaptation, and how should the robot behave during the initial adaptation stage?
% Assistant response: Distinguish a new-user cold start from having no offline dataset at all. For a
% new user, initialize the human model from a population/physics prior and adapt only compact latent
% variables online. The robot should initially use a conservative low-gain impedance or compliant
% controller, bounded speed/force, and a safety filter. It can apply small information-seeking actions
% only when they remain safe, then increase assistance as posterior confidence grows. If no offline
% human data exist, training a large response model purely online is neither data-efficient nor safely
% identifiable. Start instead from a structured dynamics/game prior and a safe default controller,
% collect data, and learn the richer population model across later interactions. 
% User decision: Include this answer as a visible backup slide for possible Q&A.
\end{frame}

\begin{frame}{Human intention is hidden but control-relevant}
\begin{columns}[T,onlytextwidth]
  \column{0.50\textwidth}
  \begin{block}{What the robot observes}
  \begin{itemize}
    \item object pose and velocity $x_t^O$;
    \item interaction wrench $\mathbf{w}_t^{\mathrm{int}}$ (3D force + 3D torque);
    \item human/robot motion and recent actions;
    \item task geometry, goal, and constraints $c$.
  \end{itemize}
  \end{block}
  \column{0.46\textwidth}
  \begin{block}{What remains hidden}
  \begin{itemize}
    \item desired object motion or local goal;
    \item leader/follower role and compliance;
    \item effort, force, and safety preferences;
    \item responses to possible robot actions.
  \end{itemize}
  \end{block}
\end{columns}
\vspace{0.25cm}
\begin{alertblock}{Central question}
How should human intention be represented and inferred so that it improves real-time robot planning and control in physically coupled tasks?
\end{alertblock}
\end{frame}

\begin{frame}{A useful model must resolve four uncertainties}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{itemize}
  \item \key{Behavioral multimodality}: the same state may admit several valid goals, paths, or roles.
  \item \key{Partial observability}: motion and force provide indirect, sometimes conflicting evidence.
\end{itemize}
\column{0.48\textwidth}
\begin{itemize}
  \item \key{Closed-loop interaction}: the human responds to the robot and may change strategy.
  \item \key{Bounded rationality}: actions may be delayed, noisy, effort-limited, or non-optimal.
\end{itemize}
\end{columns}
\vspace{0.35cm}
\begin{block}{Terminology}
``Multimodal sensing'' means multiple input types (motion, wrench, vision). 

``Multimodal behavior'' means multiple possible future human strategies.
\end{block}
\end{frame}

\begin{frame}[shrink=5]{Shared papers show human models benefit interaction}
\small
\begin{columns}[T,onlytextwidth]
\column{0.315\textwidth}
\begin{block}{Diffusion Co-Policy}
\begin{itemize}
  \item Generates several possible joint force/action sequences
  \item Demonstrates adaptation and role switching
  \item Provides the physical test setting for collaborative transport
\end{itemize}
\end{block}
\column{0.315\textwidth}
\begin{block}{Bounded-Rational Model}
\begin{itemize}
  \item Shows joint human actions can be represented by a game model
  \item Bounded rationality fits observed behavior better
  \item Leadership and adaptation affect performance
\end{itemize}
\end{block}
\column{0.315\textwidth}
\begin{block}{PACE / N-PACE}
\begin{itemize}
  \item Estimates intent/cost online while treating the peer as an adapting learner
  \item Shows that peer modeling can improve interaction policies
  \item Provides a game-theoretic interface for online control
\end{itemize}
\end{block}
\end{columns}
\vspace{0.25cm}
\begin{alertblock}{Combined insight}
Human intention is modelable and useful for control; the unresolved question is which representation is sufficiently expressive, identifiable, and transferable.
\end{alertblock}
\end{frame}

\begin{frame}{A response-conditioned hierarchy for human intention}
\begin{block}{Core contribution 1: Reusable interaction primitive}
Output: $z_H=(m,g,v,\rho)$, describing the current phase, local goal, speed/force level, and interaction role.
\end{block}
\centering\textcolor{orange}{\Large$\Downarrow$}
\vspace{0.04cm}
\begin{block}{Core contribution 2: Response to candidate robot actions}
Predict $p_\psi(u_H\mid h_t,c,\tilde u_R,z_H)$: how the human may respond to each candidate robot action $\tilde u_R$; $\psi$ denotes learned parameters. 
\end{block}
\centering\textcolor{orange}{\Large$\Downarrow$}\par
\vspace{0.04cm}
\centering{\textbf{Two control-facing realizations}}\par
\vspace{0.04cm}
\vspace{0.03cm}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{A. Cost/preferences}
Response-conditioned cost $J_H(u_R)$ and preference weights $w_H$.
\end{block}
\column{0.48\textwidth}
\begin{block}{B. Desired object motion}
Response-conditioned goal, path mode, or trajectory $\xi_H^*$.
\end{block}
\end{columns}
% Original pink request: \pinkcomment{move the second arrow in the middle of the page horzontally
% delete "Notation is introduced on the following slides."}
% Assistant response: Agreed. The second arrow should be centered between Core Contribution 2 and
% the two realization boxes. It appears beside the heading because the arrow is not terminated by an
% explicit paragraph break; adding \par after \Downarrow will center it on its own line. The notation
% sentence can be deleted because each symbol is already defined on the following technical slides.
% User decision: Implement the centered arrow and remove the notation sentence.
% User question: A, B, and C look crowded horizontally. Is it better to retain only A and B in the
% main deck and move C to backup because C is relatively vague and less connected to the shared papers?
% Assistant response: Yes, that is the stronger meeting version. A is directly connected to the
% bounded-rational and PACE/N-PACE papers; B connects naturally to Diffusion Co-Policy's multimodal
% sequence prediction. C is a valid control interface, but it currently has the weakest evidence and
% risks diluting the main argument. I would keep C as a small box on this overview only if you want to
% signal breadth, while moving its detailed slides to backup. After you confirm, the main-deck model
% comparison and first-study slides should also be revised from A--C to A--B plus a hybrid.
% User decision: Keep only A and B in the main deck, move C to backup, and revise all following
% main-deck pages to use the updated A--B structure.
\end{frame}

\begin{frame}{How the intention representations will be evaluated}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{1. Intention identifiability}
Can the model distinguish route, role, and task phase early? Measure classification accuracy, posterior confidence, and detection delay.
\end{block}
\begin{block}{2. Future prediction}
Measure trajectory displacement, wrench prediction error, likelihood of observed behavior, and coverage of different valid futures.
\end{block}
\column{0.48\textwidth}
\begin{block}{3. Closed-loop control value}
Measure task success, completion time, human effort, conflicting interaction force, safety violations, and online computation time.
\end{block}
\begin{block}{4. Transfer and adaptation}
Test unseen users, objects, obstacle layouts, and tasks; report zero-shot performance and data required for personalization.
\end{block}
\end{columns}
\end{frame}

\begin{frame}{High-level option: Reusable interaction primitives}
\begin{columns}[T,onlytextwidth]
\column{0.52\textwidth}
\begin{block}{Switching latent-state model}
\[
z_t^H=(m_t,g_t,v_t,\rho_t),\qquad m_t\in\mathcal M_{\mathrm{int}}
\]
where $\mathcal M_{\mathrm{int}}$ includes translate, rotate, hold, align, insert, and yield.
\begin{itemize}
  \item $m_t$: current interaction primitive;
  \item $g_t$: local direction, pose, or contact goal;
  \item $v_t$: desired speed or interaction-force level;
  \item $\rho_t$: leader/follower role or compliance.
\end{itemize}
\end{block}
\column{0.44\textwidth}
\begin{exampleblock}{Why it supports generalization}
Factory tasks reuse a small vocabulary: transport may be \emph{translate--rotate--align}; insertion may be \emph{approach--align--hold--insert}.
\end{exampleblock}
\begin{alertblock}{Initial primitive vocabulary}
Start with six object-level modes: translate, rotate, hold, align, insert, and yield. Define them by motion/force patterns, not task names.
\end{alertblock}
\end{columns}
\end{frame}

\begin{frame}{How primitives are identified and learned}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Identify the current primitive}
\begin{itemize}
  \item Use task-phase labels when the phase is known.
  \item Otherwise use Expectation--Maximization (EM) to infer latent switching modes from object motion, wrench, and context.
  \item Infer $p(m_t\mid h_t,c)$ online rather than committing to one mode too early.
\end{itemize}
\end{block}
\column{0.48\textwidth}
\begin{block}{Decide how many primitives are needed}
Begin with the six-mode vocabulary. Merge modes with indistinguishable motion/force responses; add a mode only when repeated residual patterns improve held-out prediction and control.
\end{block}
\begin{alertblock}{Learn continuous parameters}
Within each mode, train goal, speed/force, and role parameters from future object motion and interaction-wrench prediction.
\end{alertblock}
\end{columns}
\end{frame}

\begin{frame}{Shared response layer: Predict human accommodation}
\begin{columns}[c,onlytextwidth]
\column{0.43\textwidth}
\begin{block}{Passive prediction}
\[
p(u^H_{t:t+H}\mid h_t,c)
\]
\centering What might the human do next?
\end{block}
\column{0.08\textwidth}
\centering\Large\textcolor{orange}{$\Longrightarrow$}
\column{0.49\textwidth}
\begin{alertblock}{Response to a candidate robot action}
\[
p_\psi(u^H_{t:t+H}\mid h_t,c,\tilde u^R_{t:t+H},z_H)
\]
\centering How might the human respond if the robot executes $\tilde u^R$?
\end{alertblock}
\end{columns}
\vspace{0.25cm}
\begin{itemize}
  \item $h_t$: recent interaction history; $c$: task context; $z_H$: current primitive.
  \item $\tilde u^R$: candidate robot action sequence; $u^H$: predicted human response.
  \item The output conditions Options A and B; it is not a separate high-level intention by itself.
\end{itemize}
% User question: Could game theory (GT) also be used in this response layer, and should that be
% discussed as a potential direction?
% Assistant response: Yes. The response layer specifies the conditional prediction required by the
% controller, not one mandatory implementation. A game-theoretic realization can compute the human's
% bounded-rational best response for each candidate robot action using an inferred human cost; this is
% closely related to N-PACE. A learned realization can estimate the conditional distribution directly
% from data. The most promising direction is hybrid: use the game model for interpretable intent and
% strategic coupling, then use a learned prior or residual to capture delay, multimodality, and behavior
% not explained by the assumed cost. The next slide presents this distinction explicitly.
% User decision: Make the game-theoretic realization explicit in the visible comparison on the next page.
\end{frame}

% \begin{frame}{Explicit human models support structured interaction}
% \begin{columns}[T,onlytextwidth]
% \column{0.315\textwidth}
% \begin{block}{Implicit behavior generation}
% Diffusion Co-Policy captures multimodal joint actions, but human intention remains implicit, making it harder to interpret, personalize, or connect directly to structured control.
% \end{block}
% \column{0.315\textwidth}
% \begin{block}{Learned response model}
% Predicts $p(u^H\mid h,c,\tilde u^R)$ for each candidate robot action. It can model accommodation without assuming a human cost or learning rule.
% \end{block}
% \column{0.315\textwidth}
% \begin{block}{Game-theoretic response}
% N-PACE uses inferred costs and a dynamic game to compute bounded-rational responses and model how the human learns about the robot.
% \end{block}
% \end{columns}
% \vspace{0.22cm}
% \begin{alertblock}{Proposed combination}
% Use N-PACE for interpretable intent and peer-learning structure; use the learned response model as a population prior or residual for behavior the game model does not explain.
% \end{alertblock}
% % Original pink question: \pinkcomment{Q: this page to backup?}
% % Assistant response: I recommend keeping this page in the main deck. It is the conceptual bridge from
% % the shared papers to the proposal: Diffusion Co-Policy motivates a learned multimodal response model,
% % N-PACE motivates an interpretable game-theoretic response, and the hybrid is the proposed direction.
% % Without this page, Option A and the later N-PACE connection appear less motivated. Present it briefly
% % (about 30--45 seconds). If the main deck must be shortened further, merge its central comparison into
% % the preceding response-layer slide and move only the expanded three-column comparison to backup.
% % User decision: Keep the page in the main deck, but do not emphasize matching Diffusion Co-Policy.
% % Mention its implicit-intent limitation only to motivate the value of an explicit human model and
% % emphasize the structured learned/game-theoretic hybrid.
% \end{frame}

\begin{frame}{Option A: Infer the human's objective}
\begin{block}{Task-conditioned human cost}
\[
J_H=\sum_{k=t}^{t+H}{w_H}^{\!\top}
\phi_\psi(x_k^O,u_k^H,u_k^R,c)
\]
\end{block}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Cost and feature notation}
$J_H$: cumulative human cost; $\phi_\psi$: learned and physical cost-feature vector; $w_H$: person-specific human preference weights.
\end{block}
\column{0.48\textwidth}
\begin{block}{State and action notation}
$x_k^O$: object state; $u_k^H$: human action; $u_k^R$: candidate robot action considered by the planner. The sum covers steps $k=t$ through $t+H$ under context $c$.
\end{block}
\end{columns}
\end{frame}

\begin{frame}{Option A: Learning the cost model}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{exampleblock}{Why it is attractive}
The weights expose tradeoffs among progress, effort, interaction force, safety, and coordination. Because $u^R$ enters the features, the inferred human optimum can change with the candidate robot action. The model connects directly to IOC, PACE, and N-PACE.
\end{exampleblock}
\begin{block}{Training without cost labels}
Hierarchical MaxEnt IOC learns shared cost features \(\phi_\psi\) and latent person-specific weights \(w_H\) from demonstrations.
% Maximum-entropy inverse optimal control (IOC) learns $\phi_\psi$ from demonstrated actions; person-specific $w_H$ is inferred as a latent variable.
\end{block}
\column{0.48\textwidth}
\begin{alertblock}{Main challenges}
Different costs can explain the same action; learned features may lose physical meaning; and human behavior is not perfectly optimal.
\end{alertblock}
\begin{block}{Practical constraint}
Anchor learned features to known physical quantities so that each component of $w_H$ retains a stable interpretation online.
\end{block}
\end{columns}
\end{frame}

\begin{frame}{Option A can supply N-PACE's human model}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Offline learned representation}
\begin{itemize}
  \item Train a shared task-conditioned feature map $\phi_\psi$ from multi-person demonstrations.
  \item Learn a context-dependent prior $p(w_H\mid c)$.
  \item Freeze $\phi_\psi$ at deployment so the online parameter meaning remains stable.
\end{itemize}
\end{block}
\column{0.48\textwidth}
\begin{block}{Online N-PACE layer}
\begin{itemize}
  \item Define $J_H=\sum_k w_H^\top\phi_\psi(x_k^O,u_k^H,u_k^R,c)$.
  \item N-PACE estimates $w_H$ and how the human is updating beliefs about the robot.
  \item The inferred cost enters the dynamic-game solver to update robot actions.
\end{itemize}
\end{block}
\end{columns}
\vspace{0.25cm}
\begin{alertblock}{What the learned model contributes}
It replaces task-by-task manual feature design with a reusable cost representation; N-PACE retains peer-aware online inference and interaction-aware control.
\end{alertblock}
\end{frame}

\begin{frame}{Option B: Desired object motion as intention}
\begin{columns}[T,onlytextwidth]
\column{0.52\textwidth}
\begin{block}{Desired trajectory model}
\[
p_\psi(\xi_H^*\mid h_t,c,\tilde u^R_{t:t+H}),\qquad
\xi_H^*=\{x_{O,k}^{H,*}\}_{k=t}^{t+H}
\]
$\xi_H^*$ is the human-intended object trajectory; $x_{O,k}^{H,*}$ is its desired object state at step $k$; $\tilde u^R$ is the candidate robot action sequence. The model outputs a distribution because several responses may be valid.
\end{block}
\textbf{Possible outputs}
\begin{itemize}
  \item terminal pose or waypoint;
  \item discrete path/strategy mode;
  \item distribution over full desired trajectories.
\end{itemize}
\column{0.44\textwidth}
\begin{exampleblock}{Why it is attractive}
Directly useful as a reference for shared-object MPC; easier to label and visualize than latent cost parameters.
\end{exampleblock}
\begin{alertblock}{Main challenges}
Multiple valid trajectories; changing intention; and limited training coverage of possible robot actions.
\end{alertblock}
\end{columns}
\end{frame}

\begin{frame}{Option B: Training and control interface}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Training without a unique intent label}
Use future object motion paired with the executed robot action as a self-supervised target, but learn several possible human responses rather than one ``correct'' path. Controlled experiments can also provide route or goal labels.
\end{block}
\column{0.48\textwidth}
\begin{block}{Learning objective}
Use a diffusion loss or a loss that keeps the best among several predictions, so left/right routes and alternative rotation strategies are retained instead of averaged together.
\end{block}
\end{columns}
\vspace{0.2cm}
\begin{alertblock}{Role in the complete system}
The desired motion is a low-level reference for shared-object model predictive control (MPC). Combine it with a primitive or preference model when the robot must explain why the human changes strategy.
% Original pink question: \pinkcomment{or MPPI? from my understanding for estimated cost maybe
% sample based could be better? the optimzation of estimated cost could be not desired and even
% waster computation?}
% Assistant response: MPPI is a sampling-based MPC algorithm, so the choice is not simply MPC versus
% MPPI. If Option B outputs a desired object trajectory and the physical dynamics/cost are smooth, a
% standard trajectory-tracking MPC is the clearest baseline and can enforce object, contact, and safety
% constraints directly. MPPI becomes attractive when the planner must sample many candidate robot
% action sequences, roll each one through a multimodal human-response model, and handle nonconvex or
% nondifferentiable objectives. It avoids cost gradients and some local-minimum issues, but it still
% depends on the cost being meaningful and may require many rollouts; sampling does not correct a
% misspecified learned cost. A precise visible wording would be: "The desired motion is a reference for
% shared-object MPC; sampling-based MPPI is one candidate solver for multimodal interactive rollouts."
% Cost/model uncertainty should additionally be handled with ensembles or risk-sensitive objectives,
% physical constraints, and an independent safety filter.
% User decision: Keep the current MPC wording and retain the MPPI discussion only in source comments.
\end{alertblock}
\vspace{0.12cm}
Related: Townsend et al. (2017); Franceschi et al. (2023).
\end{frame}

\begin{frame}[shrink=4]{Each representation serves a different modeling layer}
\scriptsize
\centering
\renewcommand{\arraystretch}{1.28}
\begin{tabularx}{\textwidth}{>{\bfseries}p{2.9cm}XXXX}
\toprule
Representation & Control interface & Key strength & Main weakness & Best use \\
\midrule
\mbox{Interaction primitive} & Latent mode $z_H$ & Cross-task structure & Needs mode discovery & Task-phase inference \\
Response layer & $p(u_H\mid \tilde u_R,z_H)$ & Models accommodation & Needs diverse robot actions & Candidate-action evaluation \\
Cost / preferences & $J_H,\,w_H,\,\phi$ & Interpretable and game-compatible & Identifiability and feature design & Negotiation and tradeoffs \\
Desired trajectory & Reference $\xi_H^*$ & Direct and visual & Needs robot-action coverage & Shared-object MPC \\
\bottomrule
\end{tabularx}
% User decision: Keep "Interaction primitive" on one line in the comparison table.
\vspace{0.32cm}
\begin{alertblock}{Proposed position}
Condition both intention interfaces on candidate robot actions. The discrete primitive vocabulary is a tractable starting point, not a fixed ontology; future work can learn a continuous, task-conditioned latent representation across tasks without predefining the modes.
% User decision: Present the initial primitive vocabulary as a tentative implementation. The longer-term
% model should be more versatile and learn its latent representation without predefined primitives.
\end{alertblock}
\end{frame}

\begin{frame}{A simpler hierarchy connects intention modeling to control}
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tikzpicture}[
  node distance=0.65cm,
  box/.style={rounded corners=2pt,draw=navy,thick,fill=lightblue,align=center,minimum height=0.82cm,font=\small},
  learned/.style={box,draw=orange,fill=warmgray},
  control/.style={box,fill=lightgreen},
  arr/.style={-{Latex[length=2.2mm]},thick,draw=muted}
]
\node[box,text width=3.0cm] (obs) {Observations\\motion + interaction wrench + context};
\node[learned,text width=2.75cm,right=of obs] (mode) {Interaction primitive\\$z_H=(m,g,v,\rho)$};
\node[learned,text width=3.4cm,right=of mode] (resp) {Human response model\\$p(u_H\mid h,c,\tilde u_R,z_H)$};
\node[box,text width=3.4cm,right=of resp] (intent) {A--B control interface\\$w_H^\top\phi_\psi$ / $\xi_H^*$};
\node[control,text width=2.9cm,right=of intent] (plan) {N-PACE / MPC\\robot action $u_R$};
\draw[arr] (obs)--(mode); \draw[arr] (mode)--(resp); \draw[arr] (resp)--(intent); \draw[arr] (intent)--(plan);
\end{tikzpicture}}
\vspace{0.35cm}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Learned offline}
Primitive transitions, shared feature/trajectory representations, and the population response prior.
\end{block}
\column{0.48\textwidth}
\begin{block}{Adapted online}
Current primitive, desired motion, preference weights, and person-specific response uncertainty.
\end{block}
\end{columns}
\end{frame}


\begin{frame}[shrink=5]{Data collection supplies weak and self-supervision}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Minimum synchronized modalities}
\begin{itemize}
  \item object pose, velocity, and acceleration;
  \item 6D interaction wrench;
  \item robot state and commanded action;
  \item task goal, geometry, contact configuration;
  \item optional human body/arm pose and vision.
\end{itemize}
\end{block}
\column{0.48\textwidth}
\begin{block}{Where training signals come from}
\begin{itemize}
  \item \textbf{weak labels}: assigned goal, path condition, task phase, role instruction;
  \item \textbf{self-supervision}: future object motion and interaction wrench;
  \item \textbf{preference evidence}: choices under controlled effort/force/path tradeoffs;
  \item \textbf{latent learning}: EM / IOC likelihood when no direct label exists.
\end{itemize}
\end{block}
\end{columns}
\vspace{0.2cm}
\begin{alertblock}{Practical collection plan}
Use accessible human--human demonstrations for pretraining, then collect human--robot trials with diverse robot actions for interactive-response identification. A curated multimodal benchmark can itself support a dataset/benchmark paper.
\end{alertblock}
\end{frame}

{\setbeamertemplate{footline}{}
\begin{frame}[plain]
\centering
\vfill
{\color{navy}\LARGE\bfseries Thank you}\par
\vspace{0.35cm}
% {\color{darkgray}\large Discussion}\par
% \vspace{0.55cm}
% {\color{muted}\normalsize Which human representation is most useful for safe, responsive co-manipulation?}\par
\vfill
\end{frame}
}



% The appendix begins before the retained previous deck.
\begin{frame}[plain]
\centering
\vfill
{\color{navy}\LARGE\bfseries Follow-up Research Directions}\par
\vspace{0.35cm}
{\color{muted}\large Longer-term subprojects in sensing, control, and cross-task generalization}\par
\vfill
\end{frame}



\begin{frame}{First study: Compare intention representations}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Physical test setting}
Collaborative object transport with:
\begin{itemize}
  \item two feasible paths around an obstacle;
  \item coordinated translation and rotation around obstacles;
  \item changing leader/follower roles;
  \item synchronized object state and 6D interaction wrench.
\end{itemize}
\end{block}
\column{0.48\textwidth}
\begin{block}{Model comparison}
\begin{enumerate}
  \item passive cost and trajectory baselines (A--B);
  \item response-conditioned cost inference;
  \item response-conditioned desired trajectory;
  \item hierarchical hybrid model.
\end{enumerate}
\end{block}
\end{columns}
\vspace{0.18cm}
\begin{alertblock}{Central hypothesis}
Combining structured intention variables with robot-action-conditioned response prediction will improve early intent inference and closed-loop coordination.
\end{alertblock}
\end{frame}

\begin{frame}{Separate model quality from team performance}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Human-model quality}
\begin{itemize}
  \item path/role classification and detection delay;
  \item desired-trajectory displacement error;
  \item force/action likelihood and calibration;
  \item adaptation after strategy changes;
  \item leave-one-person-out generalization.
\end{itemize}
\end{block}
\column{0.48\textwidth}
\begin{block}{Closed-loop interaction quality}
\begin{itemize}
  \item task success and completion time;
  \item human and robot effort;
  \item unnecessary interaction force;
  \item path efficiency and smoothness;
  \item safety and comfort violations.
\end{itemize}
\end{block}
\end{columns}
\vspace{0.22cm}
\begin{exampleblock}{Critical ablations}
Motion vs. motion+wrench; passive vs. robot-action-conditioned; fixed vs. learned features; population prior vs. online personalization.
\end{exampleblock}
\end{frame}

\begin{frame}{Proposed framework for physical co-manipulation}
\begin{block}{1. Physical interaction evidence}
Object and human motion, 6D interaction wrench, task context, and candidate robot actions.
\end{block}
\centering\textcolor{orange}{\Large$\Downarrow$}\par
\begin{block}{2. Human intention and response model}
Infer a reusable human representation, then predict how the human may respond to each candidate robot action. Expose either interpretable cost/preferences (Option A) or desired object motion (Option B) to the controller.
\end{block}
\centering\textcolor{orange}{\Large$\Downarrow$}\par
\begin{block}{3. Robot planning and safe execution}
Use N-PACE or shared-object MPC to select robot actions under physical constraints, then apply an independent safety filter. Initial validation uses collaborative object transport.
\end{block}
% Original pink concern: \pinkcomment{this page could lead some questions I cannot handle. Simplify
% this, this slides only show the framework}
% Assistant response: Agreed. The open questions about sensing platforms, label design, controller
% outputs, and paper scope could redirect the meeting toward details that are not needed to establish
% the core idea. The final content slide should instead recap the proposed information flow and state
% one conservative starting experiment, without asking the audience to choose implementation details.
% User decision: Replace the discussion-question slide with a framework-only summary.
\end{frame}


\begin{frame}{Notation used throughout the discussion}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Indices and agents}
\begin{itemize}
  \item $t$: current time; $k$: a time inside the prediction horizon;
  \item $H$: number of future steps being predicted;
  \item superscripts $O$, $H$, and $R$: object, human, and robot.
\end{itemize}
\end{block}
\begin{block}{Signals}
$x$: state or pose; $u$: action or force command; $\mathbf w^{\mathrm{int}}$: measured interaction wrench.
\end{block}
\column{0.48\textwidth}
\begin{block}{Model inputs}
$h_t$: recent interaction history up to time $t$; $c$: task context, including goal, geometry, object properties, and constraints.
\end{block}
\begin{block}{Model notation}
$p(a\mid b)$: distribution of $a$ given $b$; $\psi$: learned model parameters; $*$: desired or latent quantity; $\dot{x}$: time derivative of $x$. PACE means Peer-Aware Cost Estimation; N-PACE is its nonlinear extension.
\end{block}
\end{columns}
\end{frame}


\begin{frame}{Offline priors support online personalization}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Offline: learn shared structure}
\begin{itemize}
  \item task/interaction encoders from multi-person data;
  \item trajectory modes and residual cost features;
  \item robot-action-conditioned response model;
  \item uncertainty estimates tested on unseen participants;
\end{itemize}
\end{block}
\column{0.48\textwidth}
\begin{block}{Online: update compact variables}
\begin{itemize}
  \item belief over intent mode $b_t(z_H)$;
  \item desired motion parameters $\xi_H^*$;
  \item preference weights $w_H$;
  \item optional small person-specific adapter.
\end{itemize}
\end{block}
\end{columns}
\end{frame}

\begin{frame}{Additional notation used in the subprojects}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Human-model learning}
$J_H$: human cost; $w_t^H$: time-varying human preference weights; $\phi_\psi$: learned cost features; $u_{\mathrm{future}}^H$ and $u_{\mathrm{future}}^R$: future human and robot action sequences.
\end{block}
\begin{block}{Probability model}
$p_\psi$: learned conditional distribution; $\psi$: its trained parameters. The conditioning variables are written after the vertical bar $\mid$.
\end{block}
\column{0.48\textwidth}
\begin{block}{Robot planning}
$u_R^*$: selected robot action sequence; $J_R$: robot planning cost; $\mathbb E$: average over possible human responses; $\arg\min$: the robot action that produces the smallest expected cost.
\end{block}
\begin{block}{Shared inputs}
$x_t$: system state; $h_t$: recent history; $c$: task context. Superscripts $H$ and $R$ denote human and robot quantities.
\end{block}
\end{columns}
\end{frame}

\begin{frame}{Bayesian personalization from physical interaction}
\begin{block}{Update equation}
\[
b_{t+1}(\eta_H)\propto
p(y_{t+1}\mid h_t,c,u_t^R,\eta_H)\,b_t(\eta_H)
\]
\end{block}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Belief and latent variables}
$\eta_H=(z_H,\theta_H,w_H)$ collects the human primitive, desired-motion parameters, and preference weights. $b_t(\eta_H)$ is the robot's current belief over them.
\end{block}
\column{0.48\textwidth}
\begin{block}{New physical evidence}
$y_{t+1}=(\mathbf w_t^{\mathrm{int,obs}},x_{t+1}^O)$ combines the observed wrench and next object state. $u_t^R$ is the executed robot action; $p(\cdot)$ scores how well each human model explains the evidence.
\end{block}
\end{columns}
\vspace{0.15cm}
\begin{alertblock}{Interpretation}
$\propto$ means: multiply the prior belief by the likelihood of the new observation, then normalize. Models that better predict the human--robot interaction receive more probability.
\end{alertblock}
\end{frame}

\begin{frame}{Cold start: Safe behavior before personalization}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{New user, no interaction history}
\begin{itemize}
  \item Initialize from a population and physical-dynamics prior.
  \item Adapt only compact variables: primitive, preference weights, and desired-motion parameters.
  \item Increase robot assistance only as confidence in the human model improves.
\end{itemize}
\end{block}
\column{0.48\textwidth}
\begin{block}{Initial robot behavior}
\begin{itemize}
  \item Use low-gain impedance or compliant control.
  \item Bound speed and interaction force; retain a safety filter.
  \item Apply small information-seeking actions only when they remain safe.
\end{itemize}
\end{block}
\end{columns}
\vspace{0.02cm}
\begin{alertblock}{If no offline human dataset exists}
Do not train a large response model purely online. Start from a structured dynamics/game prior and a safe default controller, collect interaction data, and learn the population model across later trials.
\end{alertblock}
\end{frame}

\begin{frame}{Backup Option C: What is an attractor model?}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Intuitive meaning}
An attractor model assumes the human is pulling the shared object toward an internal target. The predicted desired velocity points toward that target and decreases as the object approaches it.
\end{block}
\begin{block}{Position example}
\[
\dot p_O^{H,*}=K_p\bigl(p_H^*(\tilde u_R)-p_O\bigr)
\]
$p_O$: current object position; $p_H^*(\tilde u_R)$: inferred human target under candidate robot action $\tilde u_R$; $K_p>0$: response gain; $\dot p_O^{H,*}$: human-desired object velocity.
\end{block}
\column{0.48\textwidth}
\begin{exampleblock}{Why it is attractive}
The robot estimates a target and a few gains instead of assuming that the human solves a complete optimal-control problem.
\end{exampleblock}
\begin{alertblock}{What the model can and cannot express}
It is well suited to local motion toward a goal, but one attractor cannot represent several competing routes or long-term effort/safety tradeoffs.
\end{alertblock}
\end{columns}
\end{frame}

\begin{frame}{Backup Option C: Full rigid-object pose model}
\begin{block}{Response-conditioned translation and rotation}
\[
\begin{bmatrix}\dot p_O^{H,*}\\[2pt]\omega_O^{H,*}\end{bmatrix}
=f_H(p_O,R_O,c,\tilde u_R;\theta_H)
\]
$p_O$ and $R_O$ are the current object position and orientation; $\dot p_O^{H,*}$ and $\omega_O^{H,*}$ are the human-desired linear and angular velocities.
\end{block}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Translation component}
$\dot p_O^{H,*}=K_p\bigl(p_H^*(\tilde u_R)-p_O\bigr)$, where $p_H^*$ is the inferred target position and $K_p$ is the translation response gain.
\end{block}
\column{0.48\textwidth}
\begin{block}{Rotation component}
$\omega_O^{H,*}=K_{\mathrm{rot}}e_R(R_O,R_H^*)$, where $R_H^*$ is the inferred target orientation and $e_R$ converts orientation error to a 3D rotation vector.
\end{block}
\end{columns}
\end{frame}

\begin{frame}{Backup Option C: Training and online inference}
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Training without parameter labels}
Fit the likelihood of observed object motion under diverse robot actions using constrained system identification. The desired pose and response gains are latent variables rather than manually labeled quantities.
\end{block}
\column{0.48\textwidth}
\begin{block}{Switching between local models}
Use Expectation--Maximization (EM) offline or a particle filter online to infer $\theta_H$ and the active mode from motion and wrench measurements.
\end{block}
\end{columns}
\vspace{0.2cm}
\begin{alertblock}{Physical constraints}
Constrain $f_H$ so the predicted motion remains stable, feasible for the shared object, and consistent with contact and actuation limits.
\end{alertblock}
\vspace{0.12cm}
Related: constraint-aware dynamical-system intent estimation with particle filtering (Shao et al., 2024).
\end{frame}

\begin{frame}{SP1: Build the physical evidence base}
\footnotesize
\begin{columns}[T,onlytextwidth]
\column{0.49\textwidth}
\begin{block}{Theme and focus}
\textbf{Physically grounded multimodal data}
\begin{itemize}
  \item Build shared-object transport experiments
  \item Synchronize motion, object state, and force/torque
  \item Create ambiguous paths, role switching, and strategy changes
  \item Compare motion-only, wrench-only, and multimodal inference
\end{itemize}
\end{block}
\column{0.47\textwidth}
\begin{alertblock}{Challenges $\rightarrow$ solutions}
\begin{itemize}
  \item No direct intent labels $\rightarrow$ hierarchical task/strategy/action labels
  \item Noisy contact data $\rightarrow$ filtering and contact-state estimation
  \item High person variability $\rightarrow$ probabilistic latent-state models
  \item Costly data $\rightarrow$ human--human, simulation, and HRI data mixture
\end{itemize}
\end{alertblock}
\end{columns}
\vspace{0.18cm}
\paperout{A multimodal benchmark and systematic study of force/torque for human intent inference.}
\end{frame}

\begin{frame}[shrink=6]{SP2: Make intent and preference explicit}
\footnotesize
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Bounded-rational human model}
\[
J_H=\sum_t {w_t^H}^{\!\top}\phi_\psi(x_t,u_t^H,u_t^R,c)
\]
Reusable features may represent progress, pose, effort, interaction force, safety, smoothness, and coordination.
\begin{itemize}
  \item Infer intent, role, preference, and rationality online
  \item Represent the human's belief about robot intent
  \item Treat the human as another adapting learner
\end{itemize}
\end{block}
\column{0.48\textwidth}
\begin{alertblock}{Challenges $\rightarrow$ solutions}
\begin{itemize}
  \item Cost identifiability $\rightarrow$ Bayesian / maximum-entropy IOC
  \item Ambiguous intent $\rightarrow$ informative robot actions
  \item Unknown adaptation $\rightarrow$ latent learning-state estimation
  \item Online computation $\rightarrow$ local game approximations and MPC
\end{itemize}
\end{alertblock}
\end{columns}
\vspace{0.15cm}
\paperout{Peer-aware intent and preference inference for physical co-manipulation.}
\end{frame}

\begin{frame}[shrink=8]{SP3: Learn responses to possible robot actions}
\footnotesize
\begin{columns}[T,onlytextwidth]
\column{0.49\textwidth}
\begin{block}{Interactive response prediction}
\[
p_\psi(u^H_{\mathrm{future}}\mid h_t,c,u^R_{\mathrm{future}})
\]
\begin{itemize}
  \item Predict multimodal human response trajectories
  \item Condition on motion, wrench, context, and candidate robot actions
  \item Predict uncertainty and latent-state transitions
  \item Compare Transformer, diffusion, and latent-variable models
\end{itemize}
\end{block}
\column{0.47\textwidth}
\begin{alertblock}{Challenges $\rightarrow$ solutions}
\begin{itemize}
  \item Limited action coverage $\rightarrow$ diverse robot probing
  \item Correlation vs. response $\rightarrow$ counterfactual conditioning
  \item Out-of-distribution actions $\rightarrow$ uncertainty estimation and detection
  \item Slow inference $\rightarrow$ distilled or few-step diffusion
\end{itemize}
\end{alertblock}
\end{columns}
\vspace{0.15cm}
\paperout{Robot-action-conditioned diffusion models for multimodal human response prediction.}
\end{frame}

\begin{frame}{SP4: Close the loop through safe control}
\footnotesize
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Human-model-aware planning}
\[
u_R^*=\arg\min_{u_R}\;\mathbb{E}_{u_H\sim p_\psi}[J_R]
\]
subject to object dynamics, actuation, force, collision, and safety constraints.
\begin{itemize}
  \item Optimize progress, team effort, and interaction force
  \item Replan as the human model is updated
  \item Use a safety filter before execution
\end{itemize}
\end{block}
\column{0.48\textwidth}
\begin{alertblock}{Challenges $\rightarrow$ solutions}
\begin{itemize}
  \item Prediction error $\rightarrow$ short-horizon MPC
  \item Distributional complexity $\rightarrow$ scenario sampling
  \item Excess conservatism $\rightarrow$ risk-sensitive objectives
  \item Hard safety limits $\rightarrow$ control-barrier safety filter
\end{itemize}
\end{alertblock}
\end{columns}
\vspace{0.15cm}
\paperout{Uncertainty-aware planning and safe control with interactive human response models.}
\end{frame}

\begin{frame}[shrink=7]{SP5: Transfer human concepts across tasks and people}
\footnotesize
\begin{columns}[T,onlytextwidth]
\column{0.48\textwidth}
\begin{block}{Task-conditioned generalization}
Shared human latent concepts:
\begin{itemize}
  \item intent and local strategy;
  \item leader--follower role and compliance;
  \item effort, safety, and interaction preferences;
  \item reaction delay and adaptation state.
\end{itemize}
Encode goals, object dynamics, geometry, contacts, and constraints as task context. Adapt only latent states, preference weights, or a small adapter online.
\end{block}
\column{0.48\textwidth}
\begin{alertblock}{Challenges $\rightarrow$ solutions}
\begin{itemize}
  \item Different task structures $\rightarrow$ object-centric states and wrenches
  \item Limited physical data $\rightarrow$ simulation and human--human pretraining
  \item Negative transfer $\rightarrow$ structured task encoders
  \item Unstable personalization $\rightarrow$ Bayesian updates or small adapters
\end{itemize}
\end{alertblock}
\end{columns}
\vspace{0.15cm}
\paperout{A task-conditioned human model for cross-task co-manipulation and online personalization.}
\end{frame}

\begin{frame}[shrink=5]{Five subprojects answer one connected question chain}
\centering
\begin{tikzpicture}[
  node distance=0.65cm,
  pbox/.style={rounded corners=2pt,draw=navy,thick,fill=lightblue,minimum width=10.9cm,minimum height=0.68cm,align=left,text width=10.4cm,font=\small},
  arr/.style={-{Latex[length=2mm]},thick,draw=orange}
]
\node[pbox] (p1) {\textbf{SP1 --- Observe:} What physical signals reveal human intent and interaction strategy?};
\node[pbox,below=of p1] (p2) {\textbf{SP2 --- Infer:} What does the human intend, prefer, and believe about the robot?};
\node[pbox,below=of p2] (p3) {\textbf{SP3 --- Predict:} How will the human respond to candidate robot actions?};
\node[pbox,below=of p3] (p4) {\textbf{SP4 --- Act:} How should the robot coordinate safely under prediction uncertainty?};
\node[pbox,below=of p4] (p5) {\textbf{SP5 --- Transfer:} What representations generalize across people and tasks?};
\draw[arr] (p1)--(p2); \draw[arr] (p2)--(p3); \draw[arr] (p3)--(p4); \draw[arr] (p4)--(p5);
\end{tikzpicture}
\end{frame}

\begin{frame}[shrink=6]{A focused transport study can test the core hypothesis}
\begin{columns}[T,onlytextwidth]
\column{0.47\textwidth}
\begin{block}{Initial task}
A human and robot transport a shared object through an environment with multiple feasible paths.
\end{block}
\textbf{Model inputs}
\begin{itemize}
  \item Object and human motion
  \item Interaction force/torque
  \item Goal and environment geometry
  \item Candidate robot actions
\end{itemize}
\column{0.49\textwidth}
\textbf{Comparisons}
\begin{enumerate}
  \item Reactive control without a human model
  \item Passive prediction from interaction history
  \item Structured intent inference
  \item Robot-action-conditioned prediction
  \item Hybrid structured + learned model
\end{enumerate}
\textbf{Evaluation}
\begin{itemize}
  \item Prediction accuracy and calibration
  \item Success, time, and team effort
  \item Interaction force and disagreement
  \item Adaptation, safety, and comfort violations
\end{itemize}
\end{columns}
\end{frame}

\begin{frame}[shrink=5]{The program connects human understanding to robot action}
\begin{columns}[T,onlytextwidth]
\column{0.57\textwidth}
\begin{enumerate}
  \item A physically grounded multimodal benchmark
  \item Bounded-rational, peer-aware intent inference
  \item Interactive prediction of responses to robot actions
  \item Safe planning over human response distributions
  \item Cross-task generalization and online personalization
\end{enumerate}
\column{0.39\textwidth}
\begin{alertblock}{Long-term vision}
A versatile human model that connects physical interaction, human adaptation, and robot decision-making across co-manipulation scenarios.
\end{alertblock}
\end{columns}
\vspace{0.35cm}
\begin{block}{Proposed starting point}
Use a physical collaborative-transport task to test whether force/torque sensing and robot-action-conditioned prediction improve intent inference, coordination, and safe control.
\end{block}
\end{frame}

\begin{frame}{Questions to refine the research direction}
\begin{itemize}
  \item Which physical co-manipulation task is the most useful initial test case?
  \item What sensing and robotic platforms are currently available?
  \item Should the first contribution emphasize human intent inference, interactive response prediction, or planning and control?
  \item Which components of RISE Lab's existing game-theoretic framework can be extended directly to physical coupling?
  \item What level of cross-task generalization would be most valuable for the current project?
\end{itemize}
\vspace{0.35cm}
\begin{alertblock}{Discussion objective}
Identify a focused first study that contributes to the larger goal of a generalizable and interactive human model.
\end{alertblock}
\end{frame}

\end{document}
